class Durations {
  static const Duration oneSecond = Duration(seconds: 1);
  static const Duration twoSecond = Duration(seconds: 2);
  static const Duration threeSecond = Duration(seconds: 3);
  static const Duration halfSecond = Duration(milliseconds: 500);
  static const Duration fiftyMillisecond = Duration(milliseconds: 50);
  static const Duration zeroSecond = Duration(seconds: 0);
}
